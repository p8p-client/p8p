# {{ service.classMetadata.shortName }}

[← Back to Index](../../index.md)

- **API Group:** {{ service.groupVersionKind.group is same as '' ? 'Core' : service.groupVersionKind.group }}
- **API Version:** {{ service.groupVersionKind.version }}
- **Full Class Name:** `{{ service.classMetadata.name }}`

---

## Table of Contents

{% for operation in service.operations %}
- [{{ operation.name }}](#{{ operation.name }}){% if operation.isConnectType %} ⚡{% endif %}

{% endfor %}

---

## Usage

```php
use {{ service.classMetadata.name }};

${{ service.classMetadata.shortName | camel }} = $client->getApi({{ service.classMetadata.shortName }}::class);
```

## Operations

{% for operation in service.operations %}
### {{ operation.name }}

{{ operation.description }}

{% if operation.isConnectType %}
> [!NOTE]
> This service establishes a WebSocket connection instead of a standard HTTP response.
{% endif %}

**Method Signature:**
```php
${{ service.classMetadata.shortName | camel }}->{{ operation.name }}(
{% for param in operation.pathParameters %}
    {{ param.type }} ${{ param.name }},
{% endfor -%}
{%- if operation.bodyType %}
    {{ operation.bodyType }} $body
{% endif %}
{%- if operation.queryParameters|length > 0 %}
    ?array $query = null,
{% endif -%}
): {% if operation.isConnectType %}WebSocketConnection{% else %}Response<{{ operation.responseType}}>{% endif %}

```

{% if operation.pathParameters|length > 0 %}
**Parameters:**

| Name | Type | Description |
|------|------|-------------|
{% for param in operation.pathParameters %}
| `{{ param.name }}` | `{{ param.type|table_cell }}` | {{ param.description|table_cell|default('No description')|raw }} |
{% endfor %}
{% endif %}
{% if operation.bodyType %}
| `body` | `{{ operation.bodyType }}` |  |
{% endif %}

{% if operation.queryParameters|length > 0 %}
**Query Parameters:**

| Name | Type | Description |
|------|------|-------------|
{% for param in operation.queryParameters %}
| `{{ param.name }}` | `{{ param.type|table_cell }}` | {{ param.description|table_cell|default('No description')|raw }} |
{% endfor %}
{% endif %}



---

{% endfor %}


**Generated by P8P Code Generator**
