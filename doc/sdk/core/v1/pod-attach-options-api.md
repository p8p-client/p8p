# PodAttachOptionsApi

[← Back to Index](index.md)

- **API Group:** Core
- **API Version:** v1
- **Full Class Name:** `P8p\Sdk\Api\Core\V1\PodAttachOptionsApi`

---

## Table of Contents

- [connectGetPodAttach](#connectCoreV1GetNamespacedPodAttach) ⚡
- [connectPostPodAttach](#connectCoreV1PostNamespacedPodAttach) ⚡

---

## Usage

```php
use P8p\Sdk\Api\Core\V1\PodAttachOptionsApi;

$podAttachOptionsApi = $client->getApi(PodAttachOptionsApi::class);
```

## Operations

### connectGetPodAttach

connect GET requests to attach of Pod

> [!NOTE]
> This service establishes a WebSocket connection instead of a standard HTTP response.

**Method Signature:**
```php
$podAttachOptionsApi->connectGetPodAttach(
    string $name,
    string $namespace,
    ?array $query = null,
): WebSocketConnection
```

**Parameters:**

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` | name of the PodAttachOptions |
| `namespace` | `string` | object name and auth scope, such as for teams and projects |

**Query Parameters:**

| Name | Type | Description |
|------|------|-------------|
| `container` | `null\|string` | The container in which to execute the command. Defaults to only container if there is only one container in the pod. |
| `stderr` | `bool\|null` | Stderr if true indicates that stderr is to be redirected for the attach call. Defaults to true. |
| `stdin` | `bool\|null` | Stdin if true, redirects the standard input stream of the pod for this call. Defaults to false. |
| `stdout` | `bool\|null` | Stdout if true indicates that stdout is to be redirected for the attach call. Defaults to true. |
| `tty` | `bool\|null` | TTY if true indicates that a tty will be allocated for the attach call. This is passed through the container runtime so the tty is allocated on the worker node by the container runtime. Defaults to false. |



---

### connectPostPodAttach

connect POST requests to attach of Pod

> [!NOTE]
> This service establishes a WebSocket connection instead of a standard HTTP response.

**Method Signature:**
```php
$podAttachOptionsApi->connectPostPodAttach(
    string $name,
    string $namespace,
    ?array $query = null,
): WebSocketConnection
```

**Parameters:**

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` | name of the PodAttachOptions |
| `namespace` | `string` | object name and auth scope, such as for teams and projects |

**Query Parameters:**

| Name | Type | Description |
|------|------|-------------|
| `container` | `null\|string` | The container in which to execute the command. Defaults to only container if there is only one container in the pod. |
| `stderr` | `bool\|null` | Stderr if true indicates that stderr is to be redirected for the attach call. Defaults to true. |
| `stdin` | `bool\|null` | Stdin if true, redirects the standard input stream of the pod for this call. Defaults to false. |
| `stdout` | `bool\|null` | Stdout if true indicates that stdout is to be redirected for the attach call. Defaults to true. |
| `tty` | `bool\|null` | TTY if true indicates that a tty will be allocated for the attach call. This is passed through the container runtime so the tty is allocated on the worker node by the container runtime. Defaults to false. |



---



**Generated by P8P Code Generator**
